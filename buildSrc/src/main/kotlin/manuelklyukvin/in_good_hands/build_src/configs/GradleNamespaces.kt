package manuelklyukvin.in_good_hands.build_src.configs

object GradleNamespaces {
    private const val BASE = "manuelklyukvin.in_good_hands"

    const val APP = "$BASE.app"
    const val CORE = "$BASE.core"

    const val FEED = "$BASE.feed"
    const val POST = "$BASE.post"
}